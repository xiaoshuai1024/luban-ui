export type LubanInputType = 'text' | 'email' | 'tel' | 'number' | 'password';

export interface LubanSelectOption {
  label: string;
  value: string | number;
}

export interface LubanRadioOption {
  label: string;
  value: string | number;
}
